import CachedIcon from "@mui/icons-material/Cached";

const Spinner = () => {
  return (
    <div className="spinner_container">
      <CachedIcon fontSize="large" color="primary" className="spinner_icon" />
    </div>
  );
};

export default Spinner;
